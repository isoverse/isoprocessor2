% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_peaks.R
\name{find_peaks}
\alias{find_peaks}
\title{Find, integrate, and filter peaks in one call}
\usage{
find_peaks(
  time,
  intensity,
  slope_n = 5L,
  start_slope = 0.001,
  top_slope = 5e-05,
  end_slope = 0.001,
  max_peak_width = 180,
  peak_height_pct = 20,
  eval_start = NULL,
  eval_end = NULL,
  bg_history = 25L,
  bg_method = c("individual", "linear", "rolling_min"),
  bg_smooth_points = 5L,
  square_pulse_detection = TRUE,
  square_pulse_factor = 0.55,
  min_height = 0.05,
  min_height_post = 0,
  max_peak_width_post = 0,
  min_area = 0
)
}
\arguments{
\item{time}{Numeric vector. Time in seconds.}

\item{intensity}{Numeric vector. Intensity in volts (V).}

\item{slope_n}{Integer. Rolling slope regression window size
(\code{SlopeNumberOfDataPoints}). Default 5.}

\item{start_slope}{Numeric. Slope threshold for peak start detection
(\code{StartSlope}). Peak start triggers when
\code{slope1 + slope2 > 2 * start_slope}. Default 0.001.}

\item{top_slope}{Numeric. Slope threshold for peak top zero-crossing
(\code{TopSlope}). Default 5e-05.}

\item{end_slope}{Numeric. Slope threshold for peak end detection
(\code{EndSlope}). Default 0.001.}

\item{max_peak_width}{Numeric. Maximum allowed peak width in seconds
(\code{MaxPeakWidth}). Peaks exceeding this are force-ended. Default 180.}

\item{peak_height_pct}{Numeric. Percentage drop from top required before
end-slope is checked (\code{PeakHeightPercentage}). Default 20.}

\item{eval_start}{Numeric or NULL. Evaluation start time (s)
(\code{EvalStartTime}). Default NULL (use all data).}

\item{eval_end}{Numeric or NULL. Evaluation end time (s)
(\code{EvalEndTime}). Default NULL (use all data).}

\item{bg_history}{Integer. Number of preceding points for background
estimation at peak start (\code{BackgroundHistoryPoints}). Default 25.}

\item{bg_method}{Character. Background subtraction method:
\code{"individual"} (default, matches DLL BackgroundMethod.INDIVIDUAL)
smooths the pre-peak baseline with a weighted 5-point window, takes
the minimum as a constant background;
\code{"linear"} interpolates between start and end baseline values;
\code{"rolling_min"} uses the minimum of start and end intensity.}

\item{bg_smooth_points}{Integer. Number of smoothing points for the
\code{"individual"} method (\code{NumSmoothingDataPoints}, must be 5).
Default 5.}

\item{square_pulse_detection}{Logical. Enable square-pulse classification
(\code{IsSquarePulseDetectionEnabled}). Default TRUE.}

\item{square_pulse_factor}{Numeric. Rectangularity threshold
(\code{SquarePulseDetectionFactor}). Peaks with
\code{area / (time_to_top * top_height) > factor} are flagged as
square pulses (reference gas injections). Default 0.55.}

\item{min_height}{Numeric. Minimum background-subtracted peak height (V)
(\code{MinHeight}). Default 0.05.}

\item{min_height_post}{Numeric. Post-filter minimum height (V)
(\code{MinHeightPostFilter}). Applied after integration. Default 0
(disabled).}

\item{max_peak_width_post}{Numeric. Post-filter maximum peak width (s)
(\code{MaxPeakWidthPostFilter}). Default 0 (disabled).}

\item{min_area}{Numeric. Minimum integrated area (V*s). Default 0
(disabled).}
}
\value{
A tibble with one row per detected peak. See
\code{\link{integrate_peaks}} for column descriptions.
}
\description{
Convenience wrapper that chains \code{detect_peaks()},
\code{integrate_peaks()}, and \code{filter_peaks()}.
}
\examples{
\dontrun{
  library(dplyr)
  library(ggplot2)

  dat <- read_measure_data("path/to/MeasureData.bin", long = TRUE)
  m44 <- dat |> filter(mass == 44)
  peaks <- find_peaks(m44$time_s, m44$intensity_V)

  ggplot(m44, aes(time_s, intensity_V)) +
    geom_line() +
    geom_point(data = peaks, aes(x = top_time, y = top_intensity),
               colour = "red", size = 2)
}

}
