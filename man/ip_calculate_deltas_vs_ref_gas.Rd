% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_ratios.R
\name{ip_calculate_deltas_vs_ref_gas}
\alias{ip_calculate_deltas_vs_ref_gas}
\title{Calculate peak delta values versus the reference gas}
\usage{
ip_calculate_deltas_vs_ref_gas(aggregated_data, ...)
}
\arguments{
\item{aggregated_data}{an \code{ir_aggregated_data} with a \code{peaks} dataset (from
\code{\link[=ip_detect_peaks]{ip_detect_peaks()}}) that includes a \code{ref_peak} flag.}

\item{...}{named base masses for individual species (e.g. \code{CO2 = 44}), passed
on to the ratio calculation. Species not listed use their numerically lowest
measured mass.}
}
\value{
the \code{aggregated_data} with \code{ratio_name}/\code{ratio}, \code{ref_ratio} and
\code{delta_name}/\code{delta} columns added to the \code{peaks} dataset.
}
\description{
Calculate the per-peak isotope delta values relative to the reference gas for a
\code{peaks} dataset (from \code{\link[=ip_detect_peaks]{ip_detect_peaks()}}). This first calculates the peak area
ratios (via the same dynamics as the per-peak ratio calculation: each mass's
\verb{area.<unit>s} over its peak's base mass, with the base mass overridable per
species through \code{...}, defaulting to the numerically lowest mass), adding the
\code{ratio_name} and \code{ratio} columns.
}
\details{
It then determines, for each \code{ratio_name} within each analysis (\code{uidx} /
\code{analysis}), the reference ratio \code{ref_ratio} as the mean \code{ratio} of all peaks
flagged \code{ref_peak} (see \code{\link[=ip_detect_peaks]{ip_detect_peaks()}}'s
\code{flag_rectangular_peaks_as_ref_peaks}). The delta of every peak is then
\code{delta = (ratio / ref_ratio - 1) * 1000}, with a \code{delta_name} of
\code{paste0("δ", ratio_name)} (e.g. \code{"δ45/44"}). Base mass rows (and peaks
in an analysis without any reference peak) get \code{NA}.
}
