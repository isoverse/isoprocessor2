% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/background.R
\name{ip_bgrd_detector}
\alias{ip_bgrd_detector}
\alias{ip_no_bgrd_detector}
\alias{ip_individual_bgrd_detector}
\alias{ip_isodat_default_background}
\title{Background detectors}
\usage{
ip_bgrd_detector(type, detect, details = NA_character_)

ip_no_bgrd_detector()

ip_individual_bgrd_detector(
  history.s = NA_real_,
  history.pts = NA_real_,
  smooth_coefficients = 1,
  func = min
)

ip_isodat_default_background(
  history.pts = 25,
  smooth_coefficients = c(0.16, 0.22, 0.24, 0.22, 0.16)
)
}
\arguments{
\item{type}{the kind of background detector (a single non-empty string).}

\item{detect}{a function \verb{function(traces, peak_traces)} returning
\code{peak_traces} with an added \verb{bgrd.<unit>} column.}

\item{details}{an optional plain-text description of the detector (a single
string), shown by \code{\link[=print]{print()}}. Defaults to \code{NA_character_}.}

\item{history.s, history.pts}{the background window ending at each peak's first
point, as a number of seconds (\code{history.s}) or of data points
(\code{history.pts}). Supply exactly one.}

\item{smooth_coefficients}{an odd-length numeric vector of weights for a
centered moving-average smoothing of the background window. The default \code{1}
applies no smoothing.}

\item{func}{a function applied to the (smoothed) background window to produce
the background value, e.g. \link{min} (the default) or \link{max}.}
}
\value{
an \code{ip_bgrd_detector} object.
}
\description{
Background detectors describe how a peak's background (the baseline that is
subtracted from the signal) is determined. They are supplied to a peak
detector (e.g. \code{\link[=ip_slope_based_peak_detector]{ip_slope_based_peak_detector()}}) and run from within
\code{\link[=ip_detect_peaks]{ip_detect_peaks()}} after peak detection. Use the generic \code{ip_bgrd_detector()}
to build a custom one, or the built-in \code{ip_no_bgrd_detector()},
\code{ip_individual_bgrd_detector()} and \code{ip_isodat_default_background()}.

\code{ip_no_bgrd_detector()} performs no background subtraction: its \code{detect} sets
the background to \code{0} for every data point (\verb{bgrd.<unit> = 0}).

\code{ip_individual_bgrd_detector()} determines an individual background for each
peak (and each mass) from the baseline leading up to the peak. For every peak
it takes the data points in the chosen history window ending at (and
including) the peak's first point, optionally smooths them, and applies \code{func}
(the minimum by default) to get a single background value per peak. The
history window is given either as a number of seconds (\code{history.s}) or of data
points (\code{history.pts}); supply exactly one. \code{smooth_coefficients} is an
odd-length vector of (typically normalized) weights for a centered moving
average (the default \code{1} means no smoothing); the extra points needed on each
side to smooth the edge of the window are included automatically.

\code{ip_isodat_default_background()} is the Isodat-default individual background:
the minimum of the smoothed baseline over the last \code{history.pts} data points
up to each peak (5-point smoothing).
}
\details{
\code{ip_bgrd_detector()} creates an \code{ip_bgrd_detector} object: unlike an
\link{ip_peak_detector} (a tibble), it is a simple single object (a list) with
three elements:
\itemize{
\item \code{type} (character): the kind of background detector (shown by \code{\link[=print]{print()}}).
\item \code{detect} (function): a function \verb{function(traces, peak_traces)} that returns
\code{peak_traces} with an added background column \verb{bgrd.<unit>} (in the same unit
as the \verb{intensity.<unit>} column, e.g. \code{bgrd.mV}).
\item \code{details} (character): an optional plain-text summary of the detector's
parameters (shown by \code{\link[=print]{print()}}).
}
}
\examples{
# a background detector that sets the background to zero everywhere
ip_no_bgrd_detector()
ip_no_bgrd_detector()
ip_individual_bgrd_detector(history.s = 5)
ip_individual_bgrd_detector(history.pts = 25, func = min)
ip_isodat_default_background()
}
