% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_detection.R
\name{ip_detect_peaks}
\alias{ip_detect_peaks}
\title{Detect peaks in aggregated isotope data}
\usage{
ip_detect_peaks(
  aggregated_data,
  detector,
  area = c("trapezoidal", "isodat"),
  rectangularity_factor = 0.55,
  flag_rectangular_peaks_as_ref_peaks = TRUE,
  time_shift = ip_parabolic_time_shift_detector()
)
}
\arguments{
\item{aggregated_data}{an \code{ir_aggregated_data} object (from
\code{isoreader2::ir_aggregate_isofiles()}) that includes a \code{traces} dataset.}

\item{detector}{an \link{ip_peak_detector} object.}

\item{area}{the peak-area integration method, \code{"trapezoidal"} (default) or
\code{"isodat"}; see Details.}

\item{rectangularity_factor}{the rectangularity cutoff (default \code{0.55}): a peak
is flagged \code{rectangular} when \code{area / ((end.s - start.s) * amplitude)}
exceeds this value (see Details).}

\item{flag_rectangular_peaks_as_ref_peaks}{whether to add a \code{ref_peak} column
to the \code{peaks} dataset that mirrors the \code{rectangular} flag (default \code{TRUE}).}

\item{time_shift}{an \link{ip_time_shift_detector} used to add the \code{time_shift.s}
column to the \code{peaks} dataset (default \code{\link[=ip_parabolic_time_shift_detector]{ip_parabolic_time_shift_detector()}}).}
}
\value{
the \code{aggregated_data} with \code{peak_traces} (one row per data point) and
\code{peaks} (one row per peak) datasets added.
}
\description{
Apply a peak detector (see \code{\link[=ip_peak_detector]{ip_peak_detector()}}) to aggregated isotope data
(from \code{isoreader2::ir_aggregate_isofiles()}). For each detector entry the
matching traces (for that species, with \verb{start.s <= time.s < stop.s}) are
passed to the detector's \code{detect} function, and the combined detected peaks are
stored in the \code{peak_traces} dataset of the returned object.
}
\details{
Detector entries that apply to all species (\code{species = NA}) are expanded to
every species present in the data's \code{traces}. Traces with a missing \code{time.s}
or intensity are dropped, and traces are sorted by \code{time.s} before detection.

Peaks are found on the detector's detection mass, but the returned
\code{peak_traces} (and \code{peaks}) cover \strong{all} masses over each detected peak, with
a logical \code{detection_mass} column marking the mass detection ran on.

The detection, time shift and background steps run in this order (the time
shift must happen \strong{before} the background is determined, so the background
can be computed on the shift-corrected traces):
\enumerate{
\item the detector's \code{detect} function finds the peaks and returns the peak traces
with the \code{peak} and \code{detection_mass} columns (but no background column);
\item a first, simple per-peak summary is computed (without background correction)
to flag \code{rectangular} peaks (from the detection mass) and feed the time shift;
\item the \code{time_shift} detector (see \code{\link[=ip_time_shift_detector]{ip_time_shift_detector()}}) measures each
mass's apex time offset relative to the detection mass (rectangular peaks
always get \code{time_shift.s = 0}), and the shift moves each mass's peak window to
its own (shifted) peak position. The masses are \strong{not} realigned onto a
common axis - the shift deliberately leaves their windows offset. Every
original data point is kept in \code{peak_traces} (with its value), with two
logical flags: points the shift pushed out of the window are \code{tf_removed = TRUE} (kept, but excluded from the final integration), and points it brought
in - newly included real points plus one interpolated boundary point (\code{tp = NA}) at each end when the shift is not a whole number of points - are
\code{tf_added = TRUE};
\item the background detector bundled with the peak detector (\code{bgrd}) is run on the
peak traces, adding the \verb{bgrd.<unit>} column. A point-based detector (e.g.
\code{\link[=ip_individual_bgrd_detector]{ip_individual_bgrd_detector()}}) determines the background from the original
points only (excluding the \code{tf_added} points, so the interpolated \code{tp = NA}
boundaries never take part) but applies it to every row; only the final
per-peak integration excludes the \code{tf_removed} points.
}

The final per-peak summary stored in the \code{peaks} dataset has one row per
\code{uidx}/\code{analysis}/\code{species}/\code{mass}/\code{detection_mass}/\code{peak} (whichever of these
are present): \code{start.s}/\code{end.s} (time) of the peak bounds, \code{apex.s} (time of the
maximum background-subtracted height), \verb{amplitude.<unit>} (that maximum
background-subtracted height), \verb{bgrd.<unit>} (the background at the apex),
\verb{area.<unit>s} (the integrated background-subtracted peak area), \verb{bgrd.<unit>s}
(the integrated background area), the \code{rectangular} flag and the per-mass
\code{time_shift.s} (carried over from steps 2-3). The corresponding time-point
indices \code{start.idx}/\code{end.idx}/\code{apex.idx} are only included in debug mode
(\code{ip_options(debug = TRUE)}).

A peak is considered \code{rectangular} when its (raw) area fills more than
\code{rectangularity_factor} of the bounding box, i.e. \code{area / ((end.s - start.s) * amplitude) > rectangularity_factor}. Rectangularity is determined from the
detection mass and applied to all masses of the peak. Reference (e.g. on/off)
peaks tend to be rectangular, so when \code{flag_rectangular_peaks_as_ref_peaks = TRUE} (the default) an additional \code{ref_peak} column is added that mirrors
\code{rectangular}.

The \code{area} argument selects how the areas are integrated:
\itemize{
\item \code{"trapezoidal"} (the default) applies the proper trapezoidal rule and is
interval-safe: it uses the actual time spacing between points, so it is
correct even when the points are not evenly spaced.
\item \code{"isodat"} reproduces the area calculation used by Isodat (and Qtegra): it
assumes all time intervals are identical and effectively counts the first
and last data point in full, whereas the trapezoidal rule counts them at
half weight. This usually matters little because peaks tend towards zero at
their ends, but it differs slightly for peaks that do not return to baseline.
Use it to reproduce Isodat values exactly.
}
}
