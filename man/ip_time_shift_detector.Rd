% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_shift.R
\name{ip_time_shift_detector}
\alias{ip_time_shift_detector}
\alias{ip_no_time_shift_detector}
\alias{ip_parabolic_time_shift_detector}
\title{Time shift detectors}
\usage{
ip_time_shift_detector(type, detect, details = NA_character_)

ip_no_time_shift_detector()

ip_parabolic_time_shift_detector(apex_window = 9L)
}
\arguments{
\item{type}{the kind of time shift detector (a single non-empty string).}

\item{detect}{a function \verb{function(traces, peaks)} returning \code{peaks} with an
added \code{time_shift.s} column.}

\item{details}{an optional plain-text description of the detector (a single
string), shown by \code{\link[=print]{print()}}. Defaults to \code{NA_character_}.}

\item{apex_window}{the number of points (an odd integer >= 3, default \code{9}) used
for the parabolic apex fit around each peak's apex.}
}
\value{
an \code{ip_time_shift_detector} object.
}
\description{
Time shift detectors describe how the per-mass time offset of a peak (relative
to its detection mass) is determined. They are supplied to \code{\link[=ip_detect_peaks]{ip_detect_peaks()}}
and run after the peaks (and their rectangularity) have been determined, adding
a \code{time_shift.s} column to the \code{peaks} dataset. Use the generic
\code{ip_time_shift_detector()} to build a custom one, or the built-in
\code{ip_no_time_shift_detector()} and \code{ip_parabolic_time_shift_detector()}.

\code{ip_no_time_shift_detector()} applies no time shift: its \code{detect} sets
\code{time_shift.s = 0} for every peak.

\code{ip_parabolic_time_shift_detector()} estimates each (non-rectangular) peak's
sub-sample apex time on every mass trace with a parabolic fit over \code{apex_window}
points around the peak's apex index, and reports \code{time_shift.s} as the offset
of each mass's apex from the detection mass's apex (the detection mass itself
therefore gets \code{0}). Rectangular peaks always get \code{time_shift.s = 0}.
}
\details{
\code{ip_time_shift_detector()} creates an \code{ip_time_shift_detector} object: like an
\link{ip_bgrd_detector}, it is a simple single object (a list) with three elements:
\itemize{
\item \code{type} (character): the kind of time shift detector (shown by \code{\link[=print]{print()}}).
\item \code{detect} (function): a function \verb{function(traces, peaks)} that returns
\code{peaks} with an added \code{time_shift.s} column.
\item \code{details} (character): an optional plain-text summary of the detector's
parameters (shown by \code{\link[=print]{print()}}).
}
}
\examples{
ip_no_time_shift_detector()
ip_parabolic_time_shift_detector(apex_window = 9)
ip_no_time_shift_detector()
ip_parabolic_time_shift_detector(apex_window = 9)
}
